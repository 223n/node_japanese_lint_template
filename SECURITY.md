# セキュリティに関する方針

この配布物（`@223n/lint-config-ja`）の弱点を見つけたときの知らせ方と、どこまでを弱点として扱うかを書いておく。

## 対応している版

| 版 | 対応 |
| --- | --- |
| 2.x | している |
| 1.x | していない |

見るのは最新の版だけである。
古い版で見つかったものは、最新の版で直っているかを確かめ、直っていなければ最新の版として直す。
古い版に遡って直した版は出さない。

## 知らせ方

**公開のIssueには書かないでほしい。** 直る前に広まるためである。

このリポジトリの[Security](https://github.com/223n/node_japanese_lint_template/security)から、「Report a vulnerability」で非公開のまま送れる。
受け取れることは確認してある。

次のことがわかると助かる。

- どの版で起きるか
- 何をすると起きるか（設定と、動かした手順）
- 何が起きるか

返事の速さは約束できない。個人が持っているものであり、仕事の合間に見ているためである。
急ぎであれば、そのことも書いてほしい。

## どこまでを弱点として扱うか

配っているものの大半は検査の規則であり、それ自体は動かない。
実際に動くのは、語彙検査の本体（`bin/lint-vocabulary.mjs`）である。

扱うもの。

- 配布物の中のコードが、利用側の環境で意図しない動きをするもの
- 設定や辞書を読ませることで、書いていないコードが動くもの
- 依存に由来し、この配布物の使い方で実際に影響が出るもの

扱わないもの。

- 利用側が自分で書いた正規表現が、極端に遅くなったり止まらなくなったりするもの。
  語彙検査は設定に書かれた正規表現をそのまま動かす。これは書いた本人の入力であり、外から来るものではない
- `textlint`や`markdownlint`そのものの弱点。それぞれの配布元へ知らせてほしい
- 配布物に入らないものの弱点。`example/`や`test/`の依存がこれに当たる。
  直しはするが、利用側には届かないため急ぎでは扱わない

## 直すまでの流れ

1. 受け取ったことを返す
1. 再現できるかを確かめ、扱う範囲かどうかを決める
1. 直して新しい版を出す
1. GitHubのSecurity Advisoryとして公開する。知らせてくれた人の名前は、希望があれば載せる

## 普段から見ていること

| 見るもの | 道具 | いつ |
| --- | --- | --- |
| 依存の弱点 | Dependabot | 本体は毎週、見本とワークフローは毎月 |
| コードの穴 | CodeQL | main と develop への取り込み、PR、毎週 |
| 鍵の混入 | Secret scanning | 常時。push も止める |

供給経路を見る道具が出す警告については、[依存の警告](docs/依存の警告.md)に確かめた結果を書いてある。
